{
  "name": "obsidian-telegram-reminder",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
  "build": "esbuild main.ts --bundle --outfile=main.js --target=es2020 --platform=browser --external:obsidian --external:fs --external:path --external:child_process"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}

